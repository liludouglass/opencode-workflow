---
id: ${ID}
status: open
type: deferred
priority: ${PRIORITY}
parent: ${PARENT}
spec-section: "${SPEC_SECTION}"
deferred-from: ${DEFERRED_FROM}
target-after: ${TARGET_AFTER}
reason: "${REASON}"
---

# ${TITLE}

## Context

This was deferred from ${DEFERRED_FROM} because: ${REASON}

## Requirements

When this is implemented, it should:
- 

## Master Spec Reference

See section ${SPEC_SECTION} of master-spec.md.